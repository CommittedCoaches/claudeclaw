# Developer instances configuration
# Add your entry here and create a PR to get onboarded.
#
# Required fields:
#   github_username  - Your GitHub username
#
# Optional fields:
#   instance_type    - EC2 instance type (default: t3.medium)
#   slack_user_id    - Slack member ID (Slack profile -> More -> Copy member ID)
#   telegram_user_ids - List of Telegram numeric user IDs (use @userinfobot)
#   extra_repos      - Additional repos to clone beyond the shared set
#   iam_policy_arns  - Custom IAM policies (default: restricted access)
#
# Secrets (telegram_token, slack_token) are added by an admin after merge.

kevin:
  instance_type: t3.medium
  github_username: kevinvegda
  slack_user_id: "U07SZD1RJ5N"
  telegram_user_ids: [8476669195]
  extra_repos:
    - feature-ingestion
    - chronos
    - claudeclaw
    - nexus
    - aws-staging-infra
  iam_policy_arns:
    - "arn:aws:iam::aws:policy/AdministratorAccess"

# To add yourself, copy this template and fill in your details:
# your_name:
#   github_username: your-github-username
#   slack_user_id: "U..."
#   telegram_user_ids: [your_numeric_id]
#   extra_repos: []
